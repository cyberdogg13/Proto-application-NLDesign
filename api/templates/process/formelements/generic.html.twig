
<div class="input input--horizontal">
    <div>
        <label class="input__label" for="{{ property.id }}">{{ property.title }}</label>
    </div>
    <input class="input__control input__control--text input__control--l" id="{{ property.id }}" type="{{ type }}" name="properties[{{ property.name }}]" {% if property.required is defined and property.required %}required{% endif %}>
</div>

