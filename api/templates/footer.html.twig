    <footer class="footer">
        <div class="{% if application is defined and application.defaultConfiguration.configuration.colorSchemeFooter is defined  %}{{ application.defaultConfiguration.configuration.colorScheme }}{% else %}background--donkerblauw{% endif %} " style="padding: 30px 0;padding: 3rem 0;">
            <div class="container">
                <div class="row">
                    {% if application is defined
                        and application.defaultConfiguration.configuration is defined
                        and application.defaultConfiguration.configuration.footer1 is defined
                        and application.defaultConfiguration.configuration.footer1 is not empty %}
                        {% set footer1 = commonground_resource(application.defaultConfiguration.configuration.footer1) %}
                        <div class="col-xs-3">
                            {# lets deal with menu's #}
                            {% if footer1['@type'] == 'Menu' %}
                                <span class="footer__header"  >{{ menu.name }}</span>
                                <ul class="list list--links footer__nav" >
                                    {% for item in menu.menuItems %}
                                        <li><a href="{{ item.href }}">{{ item.name }}</a></li>
                                    {% endfor %}
                                </ul>
                            {% endif %}
                            {# lets deal with templates #}
                            {% if footer1['@type'] == 'Template' %}
                                {{ template.content }}
                            {% endif %}
                        </div>
                    {% endif %}

                    {% if application is defined
                        and application.defaultConfiguration.configuration is defined
                        and application.defaultConfiguration.configuration.footer2 is defined
                        and application.defaultConfiguration.configuration.footer2 is not empty %}
                        {% set footer2 = commonground_resource(application.defaultConfiguration.configuration.footer2) %}
                        <div class="col-xs-3">
                            {# lets deal with menu's #}
                            {% if footer2['@type'] == 'Menu' %}
                                <span class="footer__header"  >{{ menu.name }}</span>
                                <ul class="list list--links footer__nav" >
                                    {% for item in menu.menuItems %}
                                        <li><a href="{{ item.href }}">{{ item.name }}</a></li>
                                    {% endfor %}
                                </ul>
                            {% endif %}
                            {# lets deal with templates #}
                            {% if footer2['@type'] == 'Template' %}
                                {{ template.content }}
                            {% endif %}
                        </div>
                    {% endif %}

                    {% if application is defined
                        and application.defaultConfiguration.configuration is defined
                        and application.defaultConfiguration.configuration.footer3 is defined
                        and application.defaultConfiguration.configuration.footer3 is not empty %}
                        {% set footer3 = commonground_resource(application.defaultConfiguration.configuration.footer3) %}
                        <div class="col-xs-3">
                            {# lets deal with menu's #}
                            {% if footer3['@type'] == 'Menu' %}
                                <span class="footer__header"  >{{ menu.name }}</span>
                                <ul class="list list--links footer__nav" >
                                    {% for item in menu.menuItems %}
                                        <li><a href="{{ item.href }}">{{ item.name }}</a></li>
                                    {% endfor %}
                                </ul>
                            {% endif %}
                            {# lets deal with templates #}
                            {% if footer3['@type'] == 'Template' %}
                                {{ template.content }}
                            {% endif %}
                        </div>
                    {% endif %}

                    {% if application is defined
                        and application.defaultConfiguration.configuration is defined
                        and application.defaultConfiguration.configuration.footer4 is defined
                        and application.defaultConfiguration.configuration.footer4 is not empty %}
                        {% set footer4 = commonground_resource(application.defaultConfiguration.configuration.footer4) %}
                        <div class="col-xs-3">
                            {# lets deal with menu's #}
                            {% if footer4['@type'] == 'Menu' %}
                                <span class="footer__header"  >{{ menu.name }}</span>
                                <ul class="list list--links footer__nav" >
                                    {% for item in menu.menuItems %}
                                        <li><a href="{{ item.href }}">{{ item.name }}</a></li>
                                    {% endfor %}
                                </ul>
                            {% endif %}
                            {# lets deal with templates #}
                            {% if footer4['@type'] == 'Template' %}
                                {{ template.content }}
                            {% endif %}
                        </div>
                    {% endif %}

                </div>
            </div>
        </div>
    </footer>
